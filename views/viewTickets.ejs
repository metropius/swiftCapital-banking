<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>swiftcaptial | View Support Ticket</title>
    <meta name="description" content="Support ticket details - Admin panel" />

    <link rel="shortcut icon" href="/storage/app/public/photos/TydZYFQSbyrBue0oZ1BQzOAr8y3J4VixErlONeNz.png" />

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <script>
        document.documentElement.style.setProperty('--primary-color', '#0ea5e9');
        document.documentElement.style.setProperty('--primary-color-dark', '#0369a1');
        document.documentElement.style.setProperty('--primary-color-light', '#38bdf8');
        document.documentElement.style.setProperty('--secondary-color', '#14b8a6');
        document.documentElement.style.setProperty('--accent-color', '#ec4899');
        document.documentElement.style.setProperty('--text-color', '#111827');
        document.documentElement.style.setProperty('--bg-color', '#f9fafb');
        document.documentElement.style.setProperty('--sidebar-bg-color', '#1e293b');
    </script>

    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: {500:'#0ea5e9',600:'#0ea5e9',700:'#0369a1'},
                        secondary: {500:'#14b8a6',600:'#0f766e'},
                        accent: {500:'#ec4899',600:'#db2777'}
                    }
                }
            }
        }
    </script>

    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        body { font-family: "Graphik", system-ui, sans-serif; }
        .page-loading { position:fixed; inset:0; background:#fff; z-index:9999; transition:all .4s .2s; }
        .page-loading.active { opacity:1; visibility:visible; }
    </style>
</head>

<body class="bg-gray-50 min-h-screen">

    <div class="flex h-screen overflow-hidden" x-data="{ mobileMenuOpen: false }">

        <!-- Sidebar -->
        <div :class="mobileMenuOpen ? 'translate-x-0' : '-translate-x-full'" 
             class="fixed inset-y-0 left-0 z-50 w-64 bg-gray-900 text-white transform transition-transform duration-300 ease-in-out md:relative md:translate-x-0">
            <div class="p-6 border-b border-gray-800">
                <a href="/adminiRoute">
                    <img src="/storage/app/public/photos/I98wEMilPMHkNy8wxBtJ86dmMTWl2kK4kkPJNI9l.png" alt="Logo" class="h-10 w-auto mx-auto">
                </a>
            </div>
            <nav class="flex-1 px-4 py-6 space-y-2 overflow-y-auto">
                <a href="/adminiRoute" class="flex items-center px-4 py-3 rounded-lg hover:bg-gray-800 transition-colors">
                    <i data-lucide="home" class="w-5 h-5 mr-3"></i>
                    <span>Dashboard</span>
                </a>
                <a href="/all-cards" class="flex items-center px-4 py-3 rounded-lg hover:bg-gray-800 transition-colors">
                    <i data-lucide="credit-card" class="w-5 h-5 mr-3"></i>
                    <span>All Cards</span>
                </a>
                <a href="/allTransfer" class="flex items-center px-4 py-3 rounded-lg hover:bg-gray-800 transition-colors">
                    <i data-lucide="send" class="w-5 h-5 mr-3"></i>
                    <span>All Transfers</span>
                </a>
                <a href="/allFunding" class="flex items-center px-4 py-3 rounded-lg hover:bg-gray-800 transition-colors">
                    <i data-lucide="download" class="w-5 h-5 mr-3"></i>
                    <span>All Deposits</span>
                </a>
                <a href="/wallets" class="flex items-center px-4 py-3 rounded-lg hover:bg-gray-800 transition-colors">
                    <i data-lucide="wallet" class="w-5 h-5 mr-3"></i>
                    <span>Wallets</span>
                </a>
                <a href="/allRefund" class="flex items-center px-4 py-3 rounded-lg hover:bg-gray-800 transition-colors">
                    <i data-lucide="refresh-cw" class="w-5 h-5 mr-3"></i>
                    <span>All Refunds</span>
                </a>
                <a href="/allLoans" class="flex items-center px-4 py-3 rounded-lg hover:bg-gray-800 transition-colors">
                    <i data-lucide="landmark" class="w-5 h-5 mr-3"></i>
                    <span>All Loans</span>
                </a>
                <a href="/allTickets" class="flex items-center px-4 py-3 rounded-lg bg-blue-600/20 text-blue-400">
                    <i data-lucide="ticket" class="w-5 h-5 mr-3"></i>
                    <span>Support Tickets</span>
                </a>
                <div class="pt-6 mt-auto border-t border-gray-800">
                    <a href="/logout" class="flex items-center px-4 py-3 rounded-lg text-red-400 hover:bg-gray-800 hover:text-red-300 transition-colors">
                        <i data-lucide="log-out" class="w-5 h-5 mr-3"></i>
                        <span>Logout</span>
                    </a>
                </div>
            </nav>
        </div>

        <!-- Mobile menu toggle -->
        <button @click="mobileMenuOpen = !mobileMenuOpen" class="md:hidden fixed top-4 left-4 z-50 bg-gray-900 text-white p-3 rounded-full shadow-lg">
            <i data-lucide="menu" class="w-6 h-6"></i>
        </button>

        <!-- Main Content -->
        <div class="flex-1 flex flex-col overflow-hidden">

            <header class="bg-white shadow-sm z-10">
                <div class="px-6 py-4 flex justify-between items-center">
                    <h1 class="text-xl font-bold text-gray-900">Support Ticket Details</h1>
                    <a href="/allTickets" class="text-blue-600 hover:underline flex items-center">
                        <i data-lucide="arrow-left" class="w-4 h-4 mr-1"></i> Back to Tickets
                    </a>
                </div>
            </header>

            <main class="flex-1 overflow-y-auto p-6">

                <div class="bg-white shadow rounded-xl overflow-hidden">
                    <div class="p-6 border-b border-gray-200">
                        <div class="flex items-center space-x-4">
                            <div class="w-16 h-16 rounded-full bg-blue-100 flex items-center justify-center text-blue-600 text-2xl font-bold">
                                T
                            </div>
                            <div>
                                <h2 class="text-2xl font-bold text-gray-900">
                                    <%= ticket.subject || 'No Subject' %>
                                </h2>
                                <p class="text-gray-600">
                                    <%= ticket.status.charAt(0).toUpperCase() + ticket.status.slice(1) %> 
                                    • Priority: <%= ticket.priority || '—' %>
                                </p>
                            </div>
                        </div>
                    </div>

                    <div class="p-6 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                        <!-- Ticket Info -->
                        <div class="space-y-4">
                            <h3 class="text-lg font-semibold text-gray-800 border-b pb-2">Ticket Information</h3>
                            <div class="space-y-3">
                                <div>
                                    <span class="text-sm text-gray-500 block">Subject</span>
                                    <p class="font-medium"><%= ticket.subject || '—' %></p>
                                </div>
                                <div>
                                    <span class="text-sm text-gray-500 block">Name</span>
                                    <p class="font-medium"><%= ticket.name || '—' %></p>
                                </div>
                                <div>
                                    <span class="text-sm text-gray-500 block">Email</span>
                                    <p class="font-medium"><%= ticket.email || '—' %></p>
                                </div>
                                <div>
                                    <span class="text-sm text-gray-500 block">Priority</span>
                                    <p class="font-medium capitalize"><%= ticket.priority || '—' %></p>
                                </div>
                                <div>
                                    <span class="text-sm text-gray-500 block">Status</span>
                                    <span class="inline-flex px-3 py-1 text-sm font-medium rounded-full 
                                        <%= ticket.status === 'pending' ? 'bg-yellow-100 text-yellow-800' : 
                                           ticket.status === 'resolved' ? 'bg-green-100 text-green-800' : 
                                           'bg-gray-100 text-gray-800' %>">
                                        <%= ticket.status.charAt(0).toUpperCase() + ticket.status.slice(1) %>
                                    </span>
                                </div>
                            </div>
                        </div>

                        <!-- Message -->
                        <div class="space-y-4 md:col-span-2 lg:col-span-3">
                            <h3 class="text-lg font-semibold text-gray-800 border-b pb-2">Message</h3>
                            <div class="prose max-w-none">
                                <p class="whitespace-pre-wrap"><%= ticket.message || 'No message provided' %></p>
                            </div>
                        </div>

                        <!-- Attached Image -->
                        <div class="space-y-4">
                            <h3 class="text-lg font-semibold text-gray-800 border-b pb-2">Attached Image</h3>
                            <% if (ticket.image) { %>
                                <div class="mt-2">
                                    <img src="<%= ticket.image %>" alt="Ticket Attachment" class="max-w-full h-auto rounded-lg shadow-md object-contain">
                                </div>
                            <% } else { %>
                                <p class="text-gray-500 italic">No image attached</p>
                            <% } %>
                        </div>

                        <!-- Owner Info -->
                        <div class="space-y-4">
                            <h3 class="text-lg font-semibold text-gray-800 border-b pb-2">Submitted By</h3>
                            <div class="space-y-3">
                                <div>
                                    <span class="text-sm text-gray-500 block">Full Name</span>
                                    <p class="font-medium">
                                        <%= ticket.owner?.firstname || '—' %> 
                                        <%= ticket.owner?.lastname || '' %>
                                    </p>
                                </div>
                                <div>
                                    <span class="text-sm text-gray-500 block">Email</span>
                                    <p class="font-medium"><%= ticket.owner?.email || '—' %></p>
                                </div>
                                <div>
                                    <span class="text-sm text-gray-500 block">Phone</span>
                                    <p class="font-medium"><%= ticket.owner?.phone || '—' %></p>
                                </div>
                                <div>
                                    <span class="text-sm text-gray-500 block">Country</span>
                                    <p class="font-medium"><%= ticket.owner?.country || '—' %></p>
                                </div>
                            </div>
                        </div>

                        <!-- Timestamps -->
                        <div class="space-y-4">
                            <h3 class="text-lg font-semibold text-gray-800 border-b pb-2">Timestamps</h3>
                            <div class="space-y-3">
                                <div>
                                    <span class="text-sm text-gray-500 block">Created At</span>
                                    <p class="font-medium"><%= new Date(ticket.createdAt).toLocaleString() %></p>
                                </div>
                                <div>
                                    <span class="text-sm text-gray-500 block">Last Updated</span>
                                    <p class="font-medium"><%= new Date(ticket.updatedAt).toLocaleString() %></p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="p-6 border-t bg-gray-50 flex justify-end space-x-4">
                        <a href="/allTickets" class="px-6 py-2.5 bg-gray-600 text-white rounded-lg hover:bg-gray-700 transition">
                            Back to List
                        </a>
                    </div>
                </div>

            </main>
        </div>
    </div>

    <script>
        lucide.createIcons();

        window.addEventListener('load', () => {
            document.querySelector('.page-loading').classList.remove('active');
        });
    </script>
</body>
</html>